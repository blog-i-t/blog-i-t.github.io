function search() {
    var search = document.getElementById("search").value;
    if (search === ""){
        search = "";
        return;
    }
    else{
        var searchcon = search;
        console.log(searchcon)
    }
}